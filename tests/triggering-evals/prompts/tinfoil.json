{
  "skill": "tinfoil",
  "should_trigger": [
    "Review this endpoint — is it secure against hostile input?",
    "We're adding an API that accepts user data; review it before it ships.",
    "Parse this uploaded file — is the parser safe against malicious input?",
    "Handle the upload, and check it for injection and path traversal.",
    "Audit this webhook receiver: untrusted input reaches a SQL query.",
    "This CLI passes user arguments to a shell command. Is that a problem?",
    "We deserialize this payload straight from the request body — review it.",
    "Check the auth flow before we ship it.",
    "This diff adds a new place where external data enters the system.",
    "The agent's tool takes free-text input and runs a query with it. Is this secure?"
  ],
  "should_not_trigger": [
    "We're renaming a public function — how do we not break callers?",
    "Should we add this npm package or use the stdlib?",
    "Merge this PR, it looks fine to me.",
    "Should this feature exist given what competitors ship?",
    "Turn this vague intent into a checkable /goal condition."
  ]
}
